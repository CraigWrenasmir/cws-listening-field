# Hosting the Listening Field

The primary public gallery is [GitHub Pages](https://listeningfield.wrenasmir.com/), published directly from this repository.

Repository Settings → Pages uses **Deploy from a branch**, **gh-pages**, **/ (root)**. The source, composition definitions and all music assets stay on **main**. The publication branch contains only the checked static build, with `.nojekyll` and the existing CNAME.

For updates:

1. Finish the selected music pipeline and visual score review. Run `npm test` and `npm run build` (which includes `npm run check`).
2. Commit the source and music assets. The build must match that clean commit exactly.
3. Publish the immutable asset tag for the selected range at the reviewed source commit. Op. 231–250 retains `nocturnal-architecture`; Op. 251–270 uses `returning-waters`; Op. 271–290 uses `vernal-dances`; Op. 291–320 uses `imaginary-ensembles`; Op. 321–330 uses `singing-returns`. Never move an existing tag. `data/asset_distribution.json` also lists immutable combined-score editions. Publish and verify their tag before their catalogue; configure a new range and score edition before extending beyond the current range.
4. Make every archive with `zip_url` in `downloads/volumes.json` available at its exact GitHub Release URL. Tags are `volume-N`; upload the matching `CWS_Volume_NN.zip`. Create a new tag at the reviewed source commit before creating its Release. Growing volumes retain their URL; replace the asset only when its membership changes. Preserve closed volume assets.
5. Run `node scripts/publish-pages.mjs --publish`. It verifies the build against committed blobs, verifies every external piece asset and combined-score edition against its immutable tag and public bytes, verifies each external archive byte for byte, then advances `main` and `gh-pages` together with a non-forced atomic push. Without `--publish`, it prepares the snapshot without changing remote refs.
6. Wait for the Pages build for the returned **pagesCommit**, then compare deployed gallery data, all new scores/audio and download assets against the tested files. Record both source and Pages commits. Keep HTTPS enforced.

On the initial migration to this flow, set Pages to `gh-pages` after that branch exists, then request one Pages build (`gh api -X POST repos/CraigWrenasmir/cws-listening-field/pages/builds`). There is no custom Actions workflow or additional hosting service. The publication branch reuses the source blobs and has its own append-only commit history; the source branch history is preserved.

Earlier gallery asset URLs remain relative; Op. 231–250 use HTTPS URLs under `raw.githubusercontent.com/CraigWrenasmir/cws-listening-field/nocturnal-architecture/`. These files remain in this repository, and their names and bytes are fixed by the tag. Recording playback, seeking and fetched SVG notation were tested in a real browser using the same origin before publication. The same build can run at a domain root, a GitHub Pages project path or the selected `wrenasmir.com` subdomain. Each work has a fragment address such as `#cws-op-004-tidal-orchard`; no server rewrite rule is needed for those links.

The host should serve `.json` as JSON, `.svg` as SVG, `.pdf` as PDF and `.mp3` as MPEG audio. HTTP range requests are useful for seeking within recordings. No server runtime, database, secret or environment variable is needed for playback.

An earlier copy remains on Sites at https://cws-listening-field.wrenasmir.chatgpt.site. `.openai/hosting.json` preserves that copy's project binding. It is separate from the primary GitHub Pages deployment and does not update when GitHub changes. Future publishing should target GitHub Pages unless Craig requests otherwise.

The custom domain is `listeningfield.wrenasmir.com`, configured in GitHub Pages and the root `CNAME` file. Squarespace DNS has a CNAME record with host `listeningfield` and target `craigwrenasmir.github.io`. Preserve this record and the CNAME file during future releases. Enable and retain Enforce HTTPS once GitHub has issued the custom-domain certificate. The portable build also includes CNAME.

## Collection size

Downloads are grouped into volumes of up to 24 works. Each volume has a concatenated score PDF and an archive containing its piece assets. The original collection URLs now name Volume 01; later volumes receive numbered filenames. `downloads/index.html` and `downloads/volumes.json` always cover the full current catalogue. Adding a piece to a later volume does not rewrite a completed earlier volume.

GitHub blocks repository files above [100 MiB](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github), and a published [Pages site must remain within 1 GB](https://docs.github.com/en/pages/getting-started-with-github-pages/github-pages-limits). The build checks use earlier review thresholds: 90 MiB per file and 950 million bytes for the actual publication file set. Review actual size as the music grows. If necessary, distribute completed ZIP volumes as assets in this repository's GitHub Releases and link them from the download index; keep the main gallery on GitHub Pages. Do not silently switch hosts or rewrite repository history.

The portable `dist/` build is also the exact Pages publication. It excludes build scripts, the full source catalogue, raw per-piece JSON (retained in the repository and complete ZIPs), and ZIPs with an external Release URL. `library.json`, series metadata, every Op. 1–230 individual PDF/MP3/MIDI/MusicXML/SVG, all earlier combined score PDF URLs and original Volume 1–10 archives remain on Pages. `scripts/site-distribution.mjs` is shared by the size check, build and publisher; `scripts/test-site-distribution.mjs` checks complete runtime/download coverage. Never raise the size guard as a substitute for reviewing distribution.

The new individual PDF/MP3/MIDI/MusicXML/SVG assets remain in source and complete volume ZIPs, and are served from the immutable music tag rather than duplicated in Pages. `data/asset_distribution.json` defines this range; the shared path resolver rejects unrecognised origins and traversal. Local checks use canonical repository paths, while the catalogue keeps the public URLs. The publisher verifies tag identity and every external asset before updating the public catalogue. This keeps the existing Pages size guard and all earlier public URLs intact.

Previous verified publication: 250 works, source `fa0a4082ccf5cc94fea60f916853ef22424f6b79`, Pages `274b62bf1688f63e49a25b3abde0279397a87446`, workflow `35179968941`. The checked static footprint is 949,689,207 bytes. Before a subsequent batch, review distribution headroom and add a new immutable asset range; preserve the 231–250 tag and all closed volume bytes. That batch stopped at 250.

## Returning Waters distribution

Op. 251–270 adds a second immutable music range in the same repository, tagged `returning-waters`. The updated Volume 12 score edition is `CWS_Volume_12_Op_249_270_Scores.pdf`, served from that tag, and the growing complete ZIP retains its existing Volume 12 Release URL. The original `downloads/CWS_Volume_12_Scores.pdf` remains available as the earlier ten-page, two-work edition. Its content streams were losslessly compressed to recover headroom: all ten rendered pages were verified pixel-identical. Closed volumes and all individual earlier assets remain unchanged. The 950,000,000-byte guard is unchanged. Add later combined editions under new filenames and immutable tags, preserving earlier edition URLs.

Returning Waters verified publication: **270 works**, source `fa635f60b8e2734000c032c064ed00473f18b597`, Pages `7a10e4037360e9c9b8cfeceb768d8d2dad4079a7`, workflow `35186459018` (successful). The static footprint is 949,406,770 bytes across 1,446 files. Op. 251–270 contains 71 individually inspected PDF pages and 2,865.25 seconds of new music. All 152 new public asset URLs, gallery code/data and both external volume archives were compared byte for byte with the reviewed files over HTTPS. Both immutable music tags were verified before publication. A direct Pages build request was needed after the atomic push did not trigger one automatically; the resulting workflow matches the exact Pages commit. That batch stopped at 270.


## Vernal Dances distribution

Op. 271–290 uses a new immutable `vernal-dances` tag. Volume 12 closes at Op. 272 with a new combined score edition `CWS_Volume_12_Op_249_272_Scores.pdf`; both earlier Volume 12 PDF URLs remain unchanged. Volume 13 begins with Op. 273–290 and uses `CWS_Volume_13_Op_273_290_Scores.pdf`. Both new score editions use the new music tag. Volume 12 retains its Release archive URL with completed membership; Volume 13 receives its own `volume-13` Release archive. Preserve all closed volumes and never move an existing music tag. The publication size guard remains 950,000,000 bytes.

The reviewed 290-work build contains 1,446 public files totalling 949,761,350 bytes (238,650 bytes below the existing guard). Later growth requires another distribution review. The complete test suite and build/check passed; all five listening routes include every work exactly once, and Kinship has all 290 nodes and 289 ancestry links. The twenty new works total 67 score pages and 1,275.30 seconds. Source, Pages and workflow verification are recorded below after publication.

Verified publication: **290 works**, source `a557dc2a8c4f1440ce93adf9a89f965b0e16910a`, Pages `c3b359ab31f3cd09a132f1e544035246ccfe0f8f`, successful workflow `35305941112`. The normal push triggered this Pages build automatically. All 149 new individual/combined asset URLs and the previous Returning Waters combined edition were fetched over HTTPS and compared byte for byte; 15 deployed site/legacy download files and all three external volume archives also matched. All three music tags retain their reviewed commits; the existing Volume 12 tag was not moved. HTTPS remains enforced. Real browser checks confirmed 290 Kinship nodes in the two connected regions, Op. 271 playback with three score pages and moving highlights, pause/reset and rotation. Local browser checks also covered mixed-metre Op. 285 seeking and highlighting, the Op. 288 tenor legend, and favourite save/reload/remove with the initial state restored. These checks are separate from acoustic listening or keyboard approval. Stop at Op. 290.


## Imaginary Ensembles distribution

Op. 291–320 uses the new immutable `imaginary-ensembles` tag for individual assets and the new Volume 13 (Op. 273–296) and Volume 14 (Op. 297–320) combined score editions. Volume 13 closes at Op. 296 and retains its existing Release archive URL; Volume 14 gets a new `volume-14` Release archive. Existing music tags, earlier edition URLs and all closed volumes remain unchanged.

The 290-work site had only 238,650 bytes of headroom under the unchanged 950,000,000-byte review threshold. The catalogue now stores event values as `tuple-v1` rows, with field order `id,h,b,d,p,ps,s,e,v` (the optional voice is last). `src/library-format.js` restores the exact previous event objects for both gallery and Kinship, and accepts the older object format as well. This removes repeated JSON keys without rounding or discarding any pitches, IDs, voice assignments or timing. `scripts/test-library-format.mjs` compares every decoded event with the source catalogue and tests the legacy path. All previous asset URLs and all 2,190 baseline asset/download files are preserved. The site-size guard is unchanged.

The audio renderer now supports `f` at base velocity 78 while retaining all earlier dynamic values. `scripts/test_forte.py` verifies Op. 310's printed forte and exact MIDI voicing. Existing recordings are not regenerated.


Imaginary Ensembles is complete and publicly verified: **320 works**, comprising 200 First Studies and 120 Second Studies, with 319 exact ancestry links and the original three bridges. Op. 291–320 adds 6,697 sounded pitch onsets, 102 visually inspected score pages and 3,471.24 seconds of recordings (including room tails). All thirty works passed score/MIDI timing, engraving, ancestry, pedal, decoded-audio and whole-hand checks. All five playlist routes include every work exactly once, and the full site test suite, build and forte/staff-dynamics regressions pass. The original 290 catalogue entries, decoded runtime entries and all 2,190 baseline asset/download files are unchanged.

Reviewed music source and immutable `imaginary-ensembles`/`volume-14` tags: `2913f129b1c91695a4091fee770feb390f40dfef`. Pages snapshot: `54ad28beee2243c986a4cc6eea8a14349613ae72`. Successful Pages workflow: `35313087181`. The checked publication contains 1,447 files totalling 948,694,107 bytes, within the unchanged 950,000,000-byte guard. All 227 new and preserved external score/audio assets, 16 deployed site/legacy download files and four Release archives were fetched over HTTPS and matched byte for byte. Existing music tags and the Volume 13 tag were not moved; Volume 13 closes at Op. 296 and Volume 14 at Op. 320. Proof: `work/qa/release320-proof.json`.

Real browser checks confirmed all 320 works in both connected Kinship regions, local Op. 316 recording playback and three-page four-voice notation, seeking/highlighting, reset, rotation, and favourite save/reload/remove with the initial state restored. Public Op. 320 loaded all five score pages and played with an active notation highlight, then paused successfully. HTTPS remains enforced. These are technical playback and visual checks, separate from acoustic listening and keyboard approval. Stop at Op. 320; Second Studies continues to Op. 400 and Third Studies starts at Op. 401.


## Singing Returns distribution

Op. 321–330 uses the new immutable `singing-returns` tag. Volume 15 begins at Op. 321 and its current combined score edition is `CWS_Volume_15_Op_321_330_Scores.pdf`, served from that tag. The complete archive has a new `volume-15` Release URL. All earlier music tags, combined editions and closed Volume 1–14 files are preserved.

The checked 330-work publication contains 1,447 files and 948,857,350 bytes, leaving 1,142,650 bytes under the unchanged review threshold. All 320 earlier catalogue and decoded runtime entries, plus 2,446 baseline music/download files, remain unchanged. The ten new scores comprise 39 individually inspected pages. The exact source, Pages, workflow and public byte verification are recorded below.


Singing Returns is complete and publicly verified: **330 works**, comprising 200 First Studies and 130 Second Studies, with 329 exact ancestry links and the original three bridges. Op. 321–330 adds 2,943 sounded pitch onsets, 39 individually inspected score pages and 1,274.88 seconds of recordings including room tails. All ten pieces passed score/MIDI timing, engraving, ancestry, pedal, decoded-audio, held-hand reach and shared-key/rest checks. The five listening routes include every work exactly once; the full website test suite and production build pass. All original 320 catalogue/runtime entries and 2,446 baseline music/download files remain unchanged.

Reviewed music source and immutable `singing-returns`/`volume-15` tags: `3b2be65bd2c9dc9c223deeb2c0c3d46ca1281fbf`. Pages snapshot: `89b32ac52a5a5ca7a7c851697688ff853c177175`. Successful automatic Pages workflow: `35317679138`. All 85 new and preserved external score/audio assets, 16 deployed site/legacy download files and five Release archives were fetched over HTTPS and matched byte for byte. Earlier music tags and closed Volumes 1–14 remain unchanged. Volume 15 contains Op. 321–330; every combined-score page stream and archived file matches its inspected source. The 1,447-file publication totals 948,857,350 bytes, within the unchanged 950,000,000-byte guard. Proof: `work/qa/release330-proof.json`.

Browser checks confirmed 330 works in the two connected Kinship regions. Local Op. 330 loaded all four score pages, played with live note highlights, and sought to bar 25 in its expanded 6/4 return; favourite save/reload/remove restored the original state. Public Op. 321 loaded all three score pages and played with active notation highlights, then paused and reset to the beginning. HTTPS remains enforced. The recordings retain shared phrase timing; melodic independence is written, with no new independent-hand timing engine. These technical and visual checks are separate from acoustic listening and keyboard approval. Stop at Op. 330; Second Studies ends at 400 and Third Studies begins at 401.


## Further Weather publication plan

The authorised Op. 331–400 cycle is published in seven checked chapters of ten. Each chapter receives a new immutable `further-weather-...` music tag and new named combined-score editions. Existing music tags and earlier PDF edition URLs stay fixed. Growing Release archives gain their authorised members; closed volumes retain every byte. Volume 15 closes at 344, Volume 16 at 368, Volume 17 at 392, and the final Second Studies Volume 18 at 400.

Catalogue transport now uses lossless `tuple-v2`: canonical event IDs become measure/index pairs, hands use numeric codes, and the redundant upper pitch is reconstructed from the unchanged pitch array. All event pitches, voices, IDs and timing values decode exactly as before; legacy objects and `tuple-v1` remain readable. Integer-valued floating numbers have shorter JSON spellings without changing their values. The real Python encoder and JavaScript decoder were checked against all 330 baseline works and 50,089 events. This recovers approximately 1.025 MB of publication headroom while preserving all earlier music and download URLs and the unchanged size guard.


## Elastic Time published: Op. 331–340

The first Further Weather chapter is complete and publicly verified. The catalogue now contains 340 works (200 First Studies, 140 Second Studies), 339 exact ancestry links and the unchanged three series bridges. These ten studies add 36 individually inspected PDF pages, 2,034 sounded pitch onsets and 1,274.93 seconds of recordings. All score/MIDI, ancestry, notation, pedal, decoded-audio, held-hand and shared-key/rest checks pass. The website test suite, all five complete listening routes and production build pass. The original 330 catalogue and decoded runtime entries, plus 2,536 immutable baseline files, remain unchanged; growing Volume 15 now contains Op. 321–340 and its 75-page combined edition.

Immutable music tag `further-weather-elastic-time`: `60664f363bd1888b25f99c4edb1897236950163b`. Published source: `88ba6f3712322cfa116f646ebe8507760b978752`. Pages snapshot: `fc9f14d9f1650b40d5ae5853f1f2016f749af840`; successful automatic workflow `35442924890`. All 83 new and preserved external music/score assets, 16 deployed site/legacy download files and five Release archives matched exact local bytes over enforced HTTPS. The publication contains 1,447 files and 947,928,702 bytes, leaving 2,071,298 bytes under the unchanged guard. Existing tags and closed volumes were preserved. Proof: `work/qa/release340-proof.json`; closed-volume ledger: `work/qa/further-weather/closed-volume-ledger.json`.

Real browser checks confirmed 340 nodes in both connected Kinship regions. Local Op. 339 played with four score pages and sought into its nine-note passage; Op. 336 showed all four pages and independently highlighted its three voices, including tied fragments. Public Op. 340 loaded five score pages, played with live highlights and sought, paused and reset correctly. These are technical playback checks, separate from acoustic listening and Craig's keyboard approval. Continue through Op. 400; the remaining six chapters are authorised.


## Distant Rooms local completion: Op. 341–350

All ten works are composed and individually reviewed: Linden Meridian, Aster Vestibule, Cedar Threshold, Moss Parallax, Fennel Hinterland, Willow Refraction, Hazel Doorway, Iris Convergence, Sedge Reconciliation, Velvet Distance. The chapter contains 46 visually inspected PDF pages, 2,361 sounded pitch onsets and 1,524.63 seconds of audio. Harmonic journeys use altered-note consequences, unequal grounds, continuous cantilena, an enharmonic fork, converging counterpoint and deliberately incomplete cadences. Score/MIDI timing, exact ancestry, notation, pedal spans, held-hand reach, shared keys/rests and decoded MP3 checks pass. Kinship locally shows all 350 works in the two connected regions. Publication is verified below; listening and keyboard approval remain separate. Interwoven Songs is the next authorised chapter.


## Distant Rooms published: Op. 341–350

The second Further Weather chapter is publicly verified: 350 works, 349 exact ancestry links, 200 First Studies and 150 Second Studies, with the same three series bridges. The ten additions contain 46 visually inspected PDF pages, 2,361 sounded pitch onsets and 1,524.63 seconds of audio. All individual music checks, the complete website test suite, five full listening routes, release regressions and production build pass. The original 330 catalogue/runtime entries and 2,536 immutable baseline files remain unchanged. Volume 15 is now closed at Op. 344 with a 95-page score; Volume 16 begins with Op. 345–350 and 26 pages. Its predecessor editions and all closed archives remain preserved.

Immutable music tag `further-weather-distant-rooms` and new `volume-16` tag point to reviewed source `761b3c68b5f188525f99d3c6190f3b4e9fe6be4c`. Existing tags, including `volume-15`, were not moved. Pages commit `629c1236706adc1ed77e04e5f7336683e1d1ffb7` completed workflow `35444980681` successfully after one explicit build request. All 95 new and preserved external assets, 16 deployed site/legacy-download files and six Release archives matched local bytes over enforced HTTPS. The publication contains 1,447 files and 948,041,479 bytes, leaving 1,958,521 bytes under the unchanged guard. Proof: `work/qa/release350-proof.json`; Volume 15’s final archive hash is frozen in the closed-volume ledger.

The real local browser showed all 350 Kinship nodes in connected 200/150 regions. Op. 346 played with five score pages and three independently highlighted voices. Public Op. 350 loaded five score pages, sought into the late complete theme, advanced from 61.90 to 72.57 seconds with live highlights and buffered audio, then paused/reset to zero. These are playback checks, separate from acoustic listening and Craig’s keyboard approval. Continue with Interwoven Songs, Op. 351–360, and the remaining authorised chapters through Op. 400.


## Interwoven Songs local completion: Op. 351–360

The ten works are composed and individually checked: Quince Tandem, Larch Crossroads, Briar Braid, Celandine Interleaf, Bluebell Commonage, Ash Colloquy, Dogwood Accord, Teasel Exchange, Lupin Rendezvous and Bilberry Chorus. They contain 42 visually inspected score pages, 1,710 sounded pitch onsets and 1,301.73 seconds of recordings. Exact subject-window checks substantiate the octave exchanges, augmentation, metrical combinations, inversion, three-subject assembly, complete stretto entries, suspensions, hocket, retrograde and closing fugato. Score/MIDI, ancestry, pedal, decoded MP3, held-hand reach and shared-key/rest checks pass.

The authored forms refine the earlier proposals: Dogwood Accord puts the suspensions in the upper subject against a mobile bass; Teasel Exchange preserves pitch order while extending selected durations; Lupin Rendezvous gives the exact retrograde a newly written bass and brings its original companion back beneath the final original theme. Bilberry Chorus documents one 17-semitone right-hand relocation following a half note; the rapid-leap limit stays at nine semitones. These are structural and visual checks, separate from acoustic listening and keyboard approval. Publication checks follow; continue through the authorised Op. 400.


## Interwoven Songs published: Op. 351–360

The third Further Weather chapter is publicly verified: 360 works, 359 exact ancestry links, 200 First Studies and 160 Second Studies, with the same three series bridges. The ten additions contain 42 visually inspected score pages, 1,710 sounded pitch onsets and 1,301.73 seconds of recordings. All music checks, complete website tests, five listening routes, release regressions and production build pass. The original 330 catalogue/runtime entries, 2,536 immutable baseline files and closed Volumes 1–15 remain unchanged. Growing Volume 16 now covers Op. 345–360 with a 68-page score; its earlier edition remains available.

Reviewed source and immutable music tag `further-weather-interwoven-songs`: `45585795617c2ffb37642a4c1001915773f5f133`. Existing music/volume tags were not moved. Pages snapshot `cda151f79f8d2e4a2e63654a7eb8abd83e1a01be` completed automatic workflow `35447461772` successfully. All 92 new and preserved external assets, 16 deployed site/legacy-download files and six Release archives matched local bytes over enforced HTTPS. The publication contains 1,447 files and 948,130,882 bytes, leaving 1,869,118 bytes under the unchanged guard. Proof: `work/qa/release360-proof.json`. Volume 16 ZIP SHA-256: `daf3e4de426a824ae4d94e570530917225809a675ea8be10d5787ec4eb950cef`; combined PDF SHA-256: `98086c7462c1147c4786b8af93191870c80bc264a6e56f780cccbd63c91738b1`.

Real browser checks confirmed all 360 Kinship nodes in connected 200/160 regions. Local Op. 355 displayed five pages and played from 88.00 to 105.78 seconds with all three voices independently highlighted, then paused/reset. Public Op. 360 displayed five pages, sought into its six-beat ending at 139.26 seconds, and advanced to its natural stop at 154.65 seconds without media errors; it was reset to zero. These are playback and visual checks, separate from acoustic listening and Craig’s keyboard approval. Continue with Quiet Virtuosity, Op. 361–370, and the remaining authorised chapters through Op. 400.


### Quiet Virtuosity publication verified — Op. 361–370

Reviewed source and new `further-weather-quiet-virtuosity` / `volume-17` tags: `78fae04d4b7729afbf03b3aac858f1d0423971cc`. Pages commit: `567ce548cc24b1001ff4f30c06bf74e933814980`. GitHub Pages workflow `35450664043` completed successfully after one explicit build request when the push did not trigger a build.

The live HTTPS library contains 370 works, 369 ancestry links, First Studies 200 and Second Studies 170, with the same three series bridges. All 93 new and preserved external assets, 16 site/legacy-download files and seven Release archives match checked bytes. Original 330 catalogue/runtime entries and 2,536 immutable baseline assets remain preserved. Volumes 1–15 are unchanged. Volume 16 is now closed at Op. 345–368 (101 pages): ZIP SHA-256 `6f99fc682aacbf2fbc0ef112cd6447e3e2f57ebf2f7c0748d02f444c35747b6c`, final PDF SHA-256 `1c6ca169a3092ce78c5f2159d589c46f68989ffd815b09b6afc1b04cfae62642`; its existing tag remains at `761b3c68b5f188525f99d3c6190f3b4e9fe6be4c`. Volume 17 opens at Op. 369–370 (8 pages): ZIP SHA-256 `e082f9b8a0f1991f379a3fdd5f2bdf7f53f969394b2c6d806f82a0990e27bb1b`. Earlier combined score editions retain their immutable URLs.

All npm tests, build, release-boundary regressions and preservation audit pass. The publication contains 1,447 files / 948,250,069 bytes, retaining 1,749,931 bytes below the unchanged guard. Chapter totals are 41 visually inspected pages, 2,439 sounded pitch onsets and 1,306.70 seconds of recordings.

Real browser checks: local and public Kinship each display all 370 nodes, split 200/170. Local Op. 367 loads four score pages; playback advanced from 66.205175 to 108.280395 seconds with readyState 4, separate bass/tenor highlights and no media error. Public Op. 370 loads four score pages; playback advanced from 36.2 to 65.038585 seconds with readyState 4, treble score highlights and no media error. Both players were paused/reset to zero; the public score was closed. These checks verify media operation and notation synchronisation, not acoustic listening or keyboard approval. Exact network evidence is in ignored `work/qa/release370-proof.json`. Continue with the authorised Op. 371–400; Third Studies begins at 401, outside this batch.


### Broken Dances publication verified — Op. 371–380

Reviewed source and immutable `further-weather-broken-dances` tag: `fe12deac4355c2bbea94da32845ef9e2738e4b8a`. Pages commit: `b86f3a9b688958c117e7711d9286963d0117313f`; workflow `35453789598` completed successfully after one explicit build request when the push produced no build. The existing Volume 17 tag remains at `78fae04d4b7729afbf03b3aac858f1d0423971cc`.

The live HTTPS library contains 380 works, 379 ancestry links and connected regions of 200 First Studies / 180 Second Studies, with the same three bridges. All 105 new and preserved external assets, 16 deployed site/legacy-download files and seven Release archives match checked bytes. All baseline music, earlier catalogue/runtime entries, closed Volumes 1–16 and prior combined score editions are preserved. Volume 17 now contains Op. 369–380, 60 pages: ZIP 27,456,309 bytes, SHA-256 `521cd7628435bb312c95cb397424b6a0bfc71241dcf71d97a6cc4f8075210689`; new combined PDF SHA-256 `b60860a93f97a8e419d08c9bec9ec1c7e35d7ebf99a7cce590556c8226235bf6`.

Chapter totals: 52 visually inspected pages, 2,575 sounded pitch onsets and 1,234.85 seconds of recordings. All individual pipeline checks, six portable metric-pivot regression tests, npm tests/build, release-boundary regressions and preservation audit pass. The 1,447-file publication is 948,364,536 bytes, leaving 1,635,464 bytes below the unchanged guard. Exact network proof: `work/qa/release380-proof.json`.

Real browser checks confirmed all 380 Kinship nodes locally and publicly. Local Hazel Transfer displayed six pages and played from 37.713598 to 70.515843 seconds across its 39.4989695-second shared tempo pivot; both hand highlights moved from bar 20 to bar 31, readyState 4, no media error. Public Thistle Junction loaded six pages, advanced from 7.047638 seconds, sought to 95.75 seconds and advanced to 113.153171 seconds in the 11/8 coda; both hands highlighted, readyState 4, no media error. Both players were paused/reset to zero and their score panels closed. These verify real media operation and notation synchronisation, separately from acoustic listening and keyboard approval. Continue through the authorised Op. 381–400; do not begin Third Studies at 401 in this batch.
