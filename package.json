{
  "name": "cws-listening-field",
  "version": "0.1.0",
  "private": true,
  "description": "CWS First Studies — a rotating piano notation gallery",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4188 --bind 127.0.0.1",
    "start": "python3 -m http.server 4188 --bind 127.0.0.1",
    "check": "node --check src/app.js && node --check src/kinship.js && node --check src/kinship-layout.js && node scripts/check.mjs",
    "test": "node scripts/test-kinship.mjs && node scripts/test-ui.mjs",
    "build": "npm run check && node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "linkedom": "^0.18.12"
  }
}
